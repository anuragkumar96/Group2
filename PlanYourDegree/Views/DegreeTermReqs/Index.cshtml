@model IEnumerable<PlanYourDegree.Models.DegreeTermReq>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DegreePlanId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StudentTermId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CourseId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DegreePlanId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StudentTermId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CourseId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DegreeTermReqId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DegreeTermReqId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DegreeTermReqId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
